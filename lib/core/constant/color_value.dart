import 'package:flutter/material.dart';

const Color mainColor = Color(0xff349eff);

LinearGradient linearGradient =
    const LinearGradient(stops: [0, 0.64, 1], begin: Alignment.bottomLeft, end: Alignment.topRight, colors: [Color(0xFF349EFF), Color(0x85349EFF), Color(0xFF349EFF)]);
